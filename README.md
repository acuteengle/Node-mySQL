# NodeMySQL

Run with command "nodemon server.js" and then you can make GET, POST, PUT requests to valid routes

Contains 2 route files that are linked to Model files for Pokemon and Pokemon Type. Model files are linked to an ORM that makes db queries using promises (could also use call-back functions as an alternative)
